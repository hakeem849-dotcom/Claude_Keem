{
  "name": "pizza-preorder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Race a Hotplate drop (e.g. AK Pizza) to pre-order before it sells out.",
  "scripts": {
    "login": "node login.mjs",
    "preorder": "node preorder.mjs",
    "dry-run": "node preorder.mjs --dry-run"
  },
  "dependencies": {
    "playwright": "^1.56.0"
  }
}
