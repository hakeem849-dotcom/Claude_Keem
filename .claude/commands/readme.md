---
description: Create or refresh README documentation from the actual codebase
---

Create or update the README so it matches the current code.

1. Inspect the project (entry points, scripts, structure, dependencies, how it
   runs and deploys). Do not invent features — verify against the code.
2. Produce/refresh a README with:
   - One-line description + short overview.
   - Features (only what exists).
   - Run / build / test instructions (real commands).
   - Project structure.
   - Any deploy notes and important caveats (e.g. demo/mock data).
3. Keep tone clear and skimmable; use tables/code blocks where helpful.
4. Show a diff of what changed before saving large rewrites.

Target (file or area), if specified: $ARGUMENTS
